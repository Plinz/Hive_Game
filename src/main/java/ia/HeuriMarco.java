/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package main.java.ia;

import java.util.ArrayList;
import java.util.List;

/**
 *
 * @author marcof
 */
public class HeuriMarco {
    /*
    public double get_score (Tablo) {
    double score = 0 ;

    score += Tablo[0][5][0] * nb_total_dep(IA);
    score += Tablo[0][5][1] * nb_total_placement(IA);
    score += Tablo[0][5][2] * nb_total_mouvement(IA);
    score += Tablo[0][5][4] * nb_piece_en_main(IA);
    score += Tablo[0][5][5] * nb_piece_en_jeu(IA);
    score += Tablo[0][5][6] * nb_piece_pinned(IA);

    
    score += Tablo[1][5][0] * nb_total_dep(humain);
    score += Tablo[1][5][1] * nb_total_placement(humain);
    score += Tablo[1][5][2] * nb_total_mouvement(humain);
    score += Tablo[1][5][4] * nb_piece_en_main(humain);
    score += Tablo[1][5][5] * nb_piece_en_jeu(humain);
    score += Tablo[1][5][6] * nb_piece_pinned(humain);

            
            
    for (All_IA_Tiles) {
        score+=get_tile_h(0,tile,Tablo);
    }
    for (All_Joueur_Tiles) {
        score+=get_tile_h(1,tile,Tablo);
}
    return score;
    }
    public int get_tile_h (joueur,piece,Tablo){
        int score = 0;
        int id_tile = piece.type;
        score += Tablo[joueur][id_tile][0] * nombre_mouvements(piece);
        score += Tablo[joueur][id_tile][1] * nombre_placements(piece);
        score += Tablo[joueur][id_tile][2] * nombre_deplacements(piece);
        score += Tablo[joueur][id_tile][3] * nombre_voisins(piece);
        score += Tablo[joueur][id_tile][4] * en_main(piece);
        score += Tablo[joueur][id_tile][5] * en_jeu(piece);
        score += Tablo[joueur][id_tile][6] * est_pinne(piece);
        return score;
    }
    
    public int nb_total_dep ( Joueur) {
        
    }
    */
}
